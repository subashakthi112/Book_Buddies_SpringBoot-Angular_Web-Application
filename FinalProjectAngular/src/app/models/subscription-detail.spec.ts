import { SubscriptionDetail } from './subscription-detail';

describe('SubscriptionDetail', () => {
  it('should create an instance', () => {
    expect(new SubscriptionDetail()).toBeTruthy();
  });
});
