import { SearchBookStockPipe } from './search-book-stock.pipe';

describe('SearchBookStockPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBookStockPipe();
    expect(pipe).toBeTruthy();
  });
});
